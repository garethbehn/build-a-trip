---
description: Implement to the PRD (lifecycle stage 3)
---
You are in the **/build** stage.

Implement the component strictly to its PRD in `prds/`. 
- Use Tailwind with the Contiki token theme (`tailwind.config.js`).
- Invoke the `contiki-fidelity` and `frontend-design` skills as you build.
- For any API work, invoke `api-proxy-safety` — no client-side keys, graceful mock fallback.
- Use TTC API v4 field names in data shapes.
- Keep AI visible (reasoning, summaries, scores).

Build the whole component, not a fragment. When done, immediately run `/self-test`.
